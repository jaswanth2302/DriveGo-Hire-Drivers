import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors (Rapido Theme)
  static const Color primary = Color(0xFFFFCC0F); // Rapido Yellow
  static const Color primaryVariant = Color(0xFFE5B80B); // Darker Yellow
  static const Color secondary = Color(0xFF272324); // Dark Grey / Black

  // Backgrounds
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFF2F2F2); // Light Grey
  static const Color surfaceVariant = Color(0xFFE0E0E0);

  // Text
  static const Color textPrimary = Color(0xFF000000); // Black for contrast
  static const Color textSecondary = Color(0xFF5A5A5A); // Medium Grey
  static const Color textHint = Color(0xFF9E9E9E);

  // Status
  static const Color success = Color(0xFF28A745);
  static const Color error = Color(0xFFD93025);
  static const Color warning = Color(0xFFFFCC00);
  static const Color info = Color(0xFF17A2B8);

  // Borders
  static const Color border = Color(0xFFD1D1D1);
}
